import { DcolorDirective } from './dcolor.directive';

describe('DcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new DcolorDirective();
    expect(directive).toBeTruthy();
  });
});
